{
	"name": "@tuckshop/registry",
	"version": "0.2.1",
	"description": "Default tuckshop registry content.",
	"license": "MIT",
	"private": true,
	"scripts": {
		"build": "pnpm --filter @tuckshop/core run build && tsx ./scripts/build.ts",
		"typecheck": "pnpm --filter @tuckshop/core run build && tsc -p tsconfig.json --noEmit",
		"test": "vitest run --passWithNoTests"
	},
	"dependencies": {
		"@tuckshop/core": "workspace:*",
		"zod": "^4.4.3"
	}
}

# Require package versions to be at least three days old.
min-release-age=3
# Require every dependency install script to be explicitly approved.
strict-allow-scripts=true
# Refuse packages whose `engines` field is incompatible with the running Node.
engine-strict=true
# Require package versions to be at least three days old.
npmMinimalAgeGate: "3d"
# Verify lockfile entries against the remote registry on install.
enableHardenedMode: true
# Refuse third-party lifecycle scripts.
enableScripts: false
# Explicit audit registry for npm audit commands.
npmAuditRegistry: "https://registry.npmjs.org"
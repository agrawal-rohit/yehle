version: 2
updates:
  # Keep GitHub Actions current on the default branch
  - package-ecosystem: github-actions
    directory: /
    target-branch: {{defaultBranch}}
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
    open-pull-requests-limit: 5
    cooldown:
      default-days: 7
    labels:
      - dependencies
      - github-actions
    commit-message:
      prefix: "ci"
      include: scope

  # JS dependency updates (Dependabot uses the npm ecosystem for all Node package managers)
  - package-ecosystem: npm
    directory: /
    target-branch: {{defaultBranch}}
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
    versioning-strategy: increase
    rebase-strategy: auto
    open-pull-requests-limit: 10
    cooldown:
      default-days: 7
    labels:
      - dependencies
      - node
      - {{packageManager}}
    commit-message:
      prefix: "fix" # for production/runtime deps
      prefix-development: "chore" # for dev-only deps
      include: scope
    groups:
      runtime-deps:
        dependency-type: production
      dev-deps:
        dependency-type: development
      typescript:
        patterns:
          - "typescript"
      vitest:
        patterns:
          - "vitest"
          - "@vitest/*"
      commitlint-husky:
        patterns:
          - "@commitlint/*"
          - "husky"
          - "lint-staged"
      git-cliff:
        patterns:
          - "git-cliff"
      biome:
        patterns:
          - "@biomejs/*"
      types:
        patterns:
          - "@types/*"
    ignore:
      - dependency-name: "typescript"
        update-types:
          - "version-update:semver-major"

{
	"name": "tuckshop",
	"version": "0.2.1",
	"description": "A CLI for building and consuming tuckshop code registries.",
	"license": "MIT",
	"bin": {
		"tuckshop": "./bin/cli.mjs"
	},
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"files": [
		"dist",
		"bin",
		"registry.json",
		"r"
	],
	"scripts": {
		"build": "pnpm --filter @tuckshop/core run build && tsc -p tsconfig.json",
		"typecheck": "pnpm --filter @tuckshop/core run build && tsc -p tsconfig.json --noEmit",
		"test": "vitest run",
		"prepack": "pnpm run build && pnpm --filter @tuckshop/registry run build && cp ../registry/registry.json ./registry.json && rm -rf ./r && if [ -d ../registry/r ]; then cp -R ../registry/r ./r; fi"
	},
	"dependencies": {
		"@tuckshop/core": "workspace:*",
		"cac": "^6.7.14",
		"chalk": "^5.6.2",
		"consola": "^3.2.3"
	}
}

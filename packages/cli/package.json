{
	"name": "cheetos",
	"version": "0.2.1",
	"description": "A CLI for consuming cheetos code registries.",
	"license": "MIT",
	"bin": {
		"cheetos": "./bin/cli.mjs"
	},
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"files": [
		"dist",
		"bin",
		"registry.json",
		"r"
	],
	"scripts": {
		"build": "pnpm --filter @cheetos/core run build && tsc -p tsconfig.json",
		"typecheck": "pnpm --filter @cheetos/core run build && tsc -p tsconfig.json --noEmit",
		"test": "vitest run",
		"prepack": "pnpm run build && pnpm --filter @cheetos/registry run build && cp ../registry/registry.json ./registry.json && rm -rf ./r && if [ -d ../registry/r ]; then cp -R ../registry/r ./r; fi"
	},
	"dependencies": {
		"@clack/core": "1.4.3",
		"@clack/prompts": "^1.7.0",
		"@cheetos/core": "workspace:*",
		"cac": "^6.7.14",
		"chalk": "^5.6.2",
		"listr2": "^10.1.0"
	}
}

# @tuckshop/core

Shared primitives and utilities for `tuckshop` registries.

This package powers the default [`tuckshop`](https://www.npmjs.com/package/tuckshop) CLI and [`@tuckshop/registry`](../registry) content. Third-party registry authors can use `parseRegistryDocument` to validate a compiled `registry.json` at runtime.

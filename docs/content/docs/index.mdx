---
title: Introduction
description: The tuckshop monorepo and package layout.
---

`tuckshop` is split into a CLI, a reusable core package, and a private default registry.

Use the navigation to explore:

- **CLI**: how to run `tuckshop`, override registries, and use the default bundle
- **Registry source**: how to validate registry documents with `@tuckshop/core`

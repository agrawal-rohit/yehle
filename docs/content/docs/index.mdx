---
title: Introduction
description: The cheetos monorepo and package layout.
---

`cheetos` is split into a CLI, a reusable core package, and a private default registry.

Use the navigation to explore:

- **CLI**: how to run `cheetos`, override registries, and use the default bundle
- **Registry source**: how to validate registry documents with `@cheetos/core`
